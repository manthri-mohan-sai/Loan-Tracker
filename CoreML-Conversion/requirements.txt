# Python dependencies for convert_qwen.py
# Install with: pip install -r requirements.txt
#
# Tested with Python 3.10+ on macOS (Apple Silicon or Intel).
# coremltools requires macOS — cannot run on Windows/Linux.

torch>=2.1.0
transformers>=4.40.0
coremltools>=7.2
numpy>=1.24.0

# Optional but speeds up the HuggingFace download
huggingface_hub>=0.20.0
